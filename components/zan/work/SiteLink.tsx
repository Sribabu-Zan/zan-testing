"use client";

/**
 * The work sections' link pair. There is one implementation, in
 * components/zan/services/SiteLinks.tsx; this module keeps the import path
 * these sections already use rather than a second copy that can drift.
 */
export { SiteLink, SiteButtonLink } from "@/components/zan/services/SiteLinks";
